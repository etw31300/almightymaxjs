
describe('SelectGuildMemberInterceptor', () => {
  describe('transformOptions', () => {
    it.todo('should return all guild members no search query is provided')
    it.todo('should exclude all bots from any search results except for Max')
    it.todo('should only return the list of users the fuzzy search yields when a search query is provided')
    it.todo('should respond with no users if fuzzy search yields no results and a search query is provided')
  })
})
